name: Deploy to Server

on:
  push:
    branches:
      - pipelinetesting  # Adjust this branch as necessary

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.5.3
        with:
          ssh-private-key: ${{ secrets.EC2_SSH_KEY }}  # Ensure this matches the secret name

      - name: Run deployment script
        run: |
          ssh -o StrictHostKeyChecking=no ubuntu@testweb.onnes.in << 'EOF'
            echo "📍 Connected to EC2..."
            cd /home/ubuntu || { echo "❌ Directory not found"; exit 1; }
            ./deploy.sh pipelinetesting   # Pass the branch name directly
          EOF
